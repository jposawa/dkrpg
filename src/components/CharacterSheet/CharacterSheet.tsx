import React from "react";

import styles from "./CharacterSheet.module.scss";

export type CharacterSheetProps = {
  sheetId: string,
}

export const CharacterSheet = ({sheetId}: CharacterSheetProps) => {
  return (
    <div></div>
  )
}