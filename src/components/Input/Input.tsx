import React from "react";

import styles from "./Input.module.scss";

type InputProps = {
  id?: string;
  name?: string;
  className?: string;
  defaultValue?: unknown;
  
}

export const Input = () => {

}